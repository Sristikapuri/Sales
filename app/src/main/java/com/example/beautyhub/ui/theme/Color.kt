package com.example.beautyhub.ui.theme

import androidx.compose.ui.graphics.Color

// Beauty Hub - Skin Care Products Theme (Peach)

// Primary Colors (Peach & Coral)
val PrimaryPeach = Color(0xFFFFB6C1)  // Light Peach
val PrimaryLightPeach = Color(0xFFFFE4E1)  // Lighter Peach
val PrimaryDarkPeach = Color(0xFFFF8C69)  // Coral Peach

// Secondary Colors (Peach & Cream)
val SecondaryPeach = Color(0xFFFFDAB9)  // Peach Puff
val LightPeach = Color(0xFFFFF0F5)  // Lavender Blush
val Cream = Color(0xFFFFF8DC)  // Cornsilk
val LightCream = Color(0xFFFFFAF0)  // Floral White

// Background and Surface Colors
val BackgroundPeach = Color(0xFFFFFAF0)  // Floral White
val SurfacePeach = Color(0xFFFFFFF5)  // White with hint of yellow
val CardPeach = Color(0xFFFFF5EE)  // Seashell

// Text Colors
val PrimaryText = Color(0xFF8B4513)  // Saddle Brown for primary text
val SecondaryText = Color(0xFFCD853F)  // Peru for secondary text
val TextOnPeach = Color(0xFF8B4513)  // Saddle Brown for text on light backgrounds
val TextOnDark = Color(0xFFFFF8DC)  // Cornsilk for text on dark backgrounds

// Status Colors
val SuccessGreen = Color(0xFF90EE90)  // Light Green
val ErrorRed = Color(0xFFFF6B6B)  // Light Coral
val WarningAmber = Color(0xFFFFD700)  // Gold

// Additional Colors
val AccentPeach = Color(0xFFFFA07A)  // Light Salmon
val DarkPeach = Color(0xFFFF8C69)  // Darker Coral

// Legacy color names for compatibility
val Gold80 = PrimaryPeach
val GoldLight80 = PrimaryLightPeach
val GoldDark80 = PrimaryDarkPeach
//val Gold40 = Gold
val GoldLight40 = LightPeach
val GoldDark40 = DarkPeach

// Background and surface colors
val GrayBackground = BackgroundPeach
val GraySurface = SurfacePeach
val GrayCard = CardPeach

// Text colors
val LightGray = LightPeach
val MediumGray = SecondaryText
val DarkText = PrimaryText

// Accent colors
val AccentBlue = Color(0xFFE3F2FD)  // Light blue
val AccentGreen = Color(0xFFE8F5E9)  // Light green

// Additional colors for compatibility
val PlatinumSilver = Color(0xFFF5F5F5)  // Very light gray
val DeepBrown = PrimaryText
val WarmGray = SecondaryText

// Silver colors (kept for compatibility)
val Silver40 = Color(0xFFE0E0E0)  // Light gray
val SilverLight40 = Color(0xFFF5F5F5)  // Very light gray
val SilverDark40 = Color(0xFFBDBDBD)  // Medium gray
val GoldBackground = GrayBackground // Ivory background
val GoldSurface = GraySurface // Cream surface
val GoldCard = GrayCard // Warm white cards
val GoldAccent = Gold80
val TextOnGold = DarkText